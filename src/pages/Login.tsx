import {useState} from "react";import {ShieldCheck,ArrowRight} from "lucide-react";import {useNavigate} from "react-router-dom";import {requireSupabase} from "../lib/supabase";import {Button,Input} from "../components/ui";export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);const navigate=useNavigate();async function submit(e:React.FormEvent){e.preventDefault();setError("");setBusy(true);try{const{error}=await requireSupabase().auth.signInWithPassword({email,password});if(error)throw error;navigate("/")}catch(err){setError(err instanceof Error?err.message:"Unable to sign in.")}finally{setBusy(false)}}return <div className="grid min-h-screen place-items-center bg-[#07111f] p-5"><div className="w-full max-w-md"><div className="mb-8 flex items-center gap-3 text-white"><div className="grid size-12 place-items-center rounded-2xl bg-cyan-400 text-[#07111f]"><ShieldCheck/></div><div><p className="font-bold">VIMBA OPS</p><p className="text-xs text-slate-400">Security operations control</p></div></div><form onSubmit={submit} className="rounded-3xl bg-white p-7 shadow-2xl"><h1 className="text-2xl font-bold">Sign in</h1><p className="mt-2 text-sm text-slate-500">Use your authorized Vimba Ops account.</p><label className="mt-6 block text-xs font-semibold">Email<Input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2"/></label><label className="mt-4 block text-xs font-semibold">Password<Input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2"/></label>{error&&<p className="mt-4 rounded-xl bg-red-50 p-3 text-xs text-red-700">{error}</p>}<Button disabled={busy} className="mt-6 w-full">{busy?"Signing in…":<>Continue <ArrowRight size={16}/></>}</Button></form></div></div>}