import {useEffect,useState} from "react";import {CheckCircle2,Clock3} from "lucide-react";import {insertRow,listRows} from "../services/api";import {Button,Card,Empty} from "../components/ui";export default function AttendancePage(){const[rows,setRows]=useState<Record<string,unknown>[]>([]);const[error,setError]=useState("");async function load(){setRows(await listRows("attendance","id,employee_id,attendance_date,clock_in,clock_out,status,exception_reason"))}useEffect(()=>{load().catch(e=>setError(e.message))},[]);async function clock(){try{await insertRow("attendance",{attendance_date:new Date().toISOString().slice(0,10),clock_in:new Date().toISOString(),status:"on_duty"});await load()}catch(e){setError(e instanceof Error?e.message:"Unable to clock in.")}}return <div className="space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold text-cyan-700">Workforce control</p><h2 className="text-3xl font-bold">Attendance</h2></div><Button onClick={clock}><CheckCircle2 size={16}/>Clock in</Button></div>{error&&<Card className="p-4 text-sm text-red-700">{error}</Card>}<Card>{rows.length===0?<Empty title="No attendance records" description="Clock-in records will appear here. GPS/QR/NFC verification metadata is structured separately."/>:<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase"><tr>{["date","employee","clock in","clock out","status","exception"].map(x=><th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{rows.map(r=><tr key={String(r.id)}><td className="px-5 py-4">{String(r.attendance_date)}</td><td className="px-5 py-4">{String(r.employee_id)}</td><td className="px-5 py-4">{r.clock_in?new Date(String(r.clock_in)).toLocaleTimeString():"—"}</td><td className="px-5 py-4">{r.clock_out?new Date(String(r.clock_out)).toLocaleTimeString():"—"}</td><td className="px-5 py-4">{String(r.status)}</td><td className="px-5 py-4">{String(r.exception_reason??"—")}</td></tr>)}</tbody></table></div>}</Card><Card className="p-5"><div className="flex gap-3"><Clock3 className="text-cyan-700"/><div><h3 className="font-bold">Verification boundary</h3><p className="mt-1 text-sm text-slate-500">Location evidence is stored separately and must be validated by a trusted provider; client coordinates are not authorization.</p></div></div></Card></div>}