import {useEffect,useState} from "react";import {MapPinned,Radio} from "lucide-react";import {listRows} from "../services/api";import {Card,Empty} from "../components/ui";export default function PatrolPage(){const[routes,setRoutes]=useState<Record<string,unknown>[]>([]);const[error,setError]=useState("");useEffect(()=>{listRows("patrol_routes","id,name,site_id,status").then(setRoutes).catch(e=>setError(e.message))},[]);return <div className="space-y-6"><div><p className="text-sm font-semibold text-cyan-700">Field assurance</p><h2 className="text-3xl font-bold">Patrols & Guard Tours</h2></div><Card className="p-5"><div className="flex gap-3"><MapPinned className="text-cyan-700"/><div><h3 className="font-bold">Checkpoint architecture</h3><p className="mt-1 text-sm text-slate-500">GPS, QR and NFC evidence can be recorded without treating a client device as an authorization source.</p></div></div></Card><Card>{error?<div className="p-5 text-sm text-red-700">{error}</div>:routes.length===0?<Empty title="No patrol routes" description="Configure routes and checkpoints per site to activate monitoring."/>:<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase"><tr>{["route","site","status"].map(x=><th key={x} className="px-5 py-3">{x}</th>)}</tr></thead><tbody className="divide-y">{routes.map(r=><tr key={String(r.id)}><td className="px-5 py-4 font-medium">{String(r.name)}</td><td className="px-5 py-4">{String(r.site_id)}</td><td className="px-5 py-4">{String(r.status)}</td></tr>)}</tbody></table></div>}</Card><Card className="p-5"><div className="flex gap-3"><Radio className="text-slate-500"/><div><h3 className="font-bold">Operational-only scope</h3><p className="mt-1 text-sm text-slate-500">No weapons or armed-response functionality is included.</p></div></div></Card></div>}