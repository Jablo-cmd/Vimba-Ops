import type {Role} from "./lib/roles";export type Profile={id:string;user_id:string;tenant_id:string|null;client_id:string|null;full_name:string;role:Role;status:"active"|"inactive"|"pending"|"suspended"|"archived";employee_id:string|null};export type DashboardMetrics={sites:number;guards:number;onDuty:number;openIncidents:number;unresolvedAlerts:number;complianceExceptions:number};